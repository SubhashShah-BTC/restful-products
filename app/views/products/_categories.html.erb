<table>
  <% categories.each do |category| %>
    <tr>
      <td><%= check_box_tag "product[category_ids][]", category.id, product.categories.include?(category) %></td>
      <td><%= category.name %></td>
    </tr>
  <% end %>
</table>